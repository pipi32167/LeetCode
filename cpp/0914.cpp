// #include <stdlib.h>
// #include <memory.h>
#include <stdio.h>
#include <assert.h>
#include <vector>

using std::vector;

class Solution
{
public:
  bool hasGroupsSizeX(vector<int> &deck)
  {
    // const vector<int>::iterator &begin = deck.begin();
    // const vector<int>::iterator &end = deck.end();
    // vector<int>::iterator *it;
    // for (it = begin; it != end; it++)
    // {
    //   /* code */
    // }
    return false;
  }
};

int main(int argc, char const *argv[])
{
  Solution *s = new Solution();
  delete s;

  // vector<int> *deck = new vector<int>();
  // deck->push_back(1);
  // deck->push_back(2);
  // assert(!s->hasGroupsSizeX(*deck));

  return 0;
}